run-name: Project automate

on:
  issues: 
    types: ['opened', 'reopened']
  
  pull_request: 
    types: ['opened', 'reopened']

jobs:
  add-to-project:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        id: checkout-repo
        uses: actions/checkout@v4

      - name: Get project information
        id: get-project-info
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ACCESS_TOKEN }}
          script: |
            // Check if issue is alredy added to any of the github project
            const resp = await github.graphql(`{
              repository(owner:"${{ github.repository_owner }}", name:"${{ github.event.repository.name }}") {
                issue(number: ${{ github.event.issue.number }}) {
                  projectItems(first: 100) {
                    totalCount
                  }
                }
              }
            }`)
            const totalCount = resp.repository.issue.projectItems.totalCount
            if( totalCount > 0 ) {
              console.log("Issue is alredy added to one of the github projects.");
              return
            }

            // Issue is not added to any of the github projects and hence continues

            // Find project number from project name
            const projects_resp = await github.graphql(`{
              organization(login:"${{ github.repository_owner }}") {
                projectsV2(first: 10) {
                  nodes {
                    number,
                    title,
                    id,
                    url
                  }
                }
              },
              repository(owner:"${{ github.repository_owner }}", name:"${{ github.event.repository.name }}") {
                issue(number: ${{ github.event.issue.number }}) {
                  projectItems(first: 10) {
                    totalCount
                  }
                }
              }
            }`)
            const prj = organization.projectsV2.nodes.filter((node) => node.title === ${{ vars.MAIN_PRJ }})
            console.log(prj)
            await exec.exec("echo" [ "PRJ_NUMBER=" + prj.number, "PRJ_URL=" + prj.url, ">> $GITHUB_OUTPUT"]);
            console.log("Step output updated successfully")


      - name: Add issue to "🆕 New"
        id: add-issue-to-new
        if: ${{ github.event_name }} == 'issues' && ${{ github.event.action }} != ''
        uses: konradpabjan/actions-add-new-issue-to-column@v1.1
        with:
          action-token: ${{ secrets.ACCESS_TOKEN }}
          project-url: ${{ steps.get-project-info.outputs.PRJ_URL }}
          column-name: "🆕 New"

      - name: Print context
        if: always()
        run: echo ${{ toJSON(steps) }}
